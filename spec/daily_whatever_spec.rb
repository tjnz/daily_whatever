require 'spec_helper'

describe DailyWhatever do
  it 'has a version number' do
    expect(DailyWhatever::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
